{
	"version": 2,
	"functions": {
		"api": {
			"maxDuration": 5,
			"memory": 512,
			"runtime": "now-node@1.0.0"
		}
	},
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api"
		},
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}

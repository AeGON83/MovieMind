{
	"version": 2,
	"functions": {
		"api": {
			"maxDuration": 5,
			"memory": 512,
			"runtime": "@vercel/node"
		}
	},
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api"
		},
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}

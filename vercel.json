{
	"version": 2,
	"functions": {
		"api": {
			"maxDuration": 30,
			"memory": 512,
			"runtime": "@vercel/node"
		}
	},
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api"
		},
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}
